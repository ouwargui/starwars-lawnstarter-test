# Application
APP_NAME="SWStarter"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000

# Logging
LOG_CHANNEL=stderr
LOG_LEVEL=warning

# Database (SQLite)
DB_CONNECTION=sqlite

# Cache (using SQLite via database driver)
CACHE_STORE=database

# Session (using SQLite via database driver)
SESSION_DRIVER=database
SESSION_LIFETIME=120

# Queue (using SQLite via database driver)
QUEUE_CONNECTION=database

# Vite public environment variables
VITE_APP_NAME="${APP_NAME}"

# SWAPI client configuration
SWAPI_BASE_URL=https://swapi.tech/api
SWAPI_TIMEOUT_SECONDS=10
SWAPI_RETRY_ATTEMPTS=3
SWAPI_RETRY_DELAY_MS=1000

# Statistics job configuration
STATISTICS_RETENTION_DAYS=30
STATISTICS_TOP_LIMIT=5
STATISTICS_PRUNE_CHUNK_SIZE=1000
